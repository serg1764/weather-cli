import { homedir } from 'os';
import { join/*, basename, dirname, extname, relative, isAbsolute, resolve, sep */} from 'path';

const filwPath = join('', 'D:\\study\\Node\\weather-cli\\weather-data.json');
const saveKeyValue = (key, value) => {
    console.log();
};

export { saveKeyValue }